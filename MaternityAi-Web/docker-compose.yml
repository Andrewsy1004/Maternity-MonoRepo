services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:8000}
    container_name: maternity-frontend
    restart: always
    ports:
      - "${PORT:-80}:80"
